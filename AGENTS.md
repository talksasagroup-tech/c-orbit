## Project conventions

- Use bun for all package management (`bun install`, `bun add`, `bun dev`).
- New routes follow the existing TanStack Router file-naming pattern.
- New components go through the existing shadcn/ui setup in `src/components`.
- Keep using and extending `src/lib/mock-data.ts` until we connect the real Go
  backend.
